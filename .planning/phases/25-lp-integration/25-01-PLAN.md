---
phase: 25-lp-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models.py
  - alembic/versions/007_lp_integration_schema.py
  - app/ui/router.py
  - app/ui/templates/lp_review.html
  - app/ui/templates/partials/lp_stage_controls.html
autonomous: true

must_haves:
  truths:
    - "LP review page shows each module (headline, hero, CTA, benefits) as a separate card"
    - "Each LP module can be individually approved"
    - "LP review is locked when the linked UGCJob is not yet approved"
    - "LP copy content is stored in DB at generation time for display in review cards"
  artifacts:
    - path: "alembic/versions/007_lp_integration_schema.py"
      provides: "Migration adding ugc_job_id, lp_module_approvals, lp_hero_image_path, lp_hero_candidate_path, lp_review_locked, lp_copy to landing_pages"
      contains: "add_column"
    - path: "app/models.py"
      provides: "LandingPage model with new LP integration columns"
      contains: "lp_module_approvals"
    - path: "app/ui/templates/lp_review.html"
      provides: "LP module review page with per-module cards and stage gate"
      contains: "lp_module_approvals"
    - path: "app/ui/templates/partials/lp_stage_controls.html"
      provides: "HTMX partial for LP approve buttons"
      contains: "hx-post"
  key_links:
    - from: "app/ui/router.py"
      to: "app/models.py"
      via: "LandingPage query with ugc_job_id join to UGCJob"
      pattern: "select.*LandingPage.*ugc_job_id"
    - from: "app/ui/templates/lp_review.html"
      to: "app/ui/router.py"
      via: "hx-post to module approve endpoint"
      pattern: "hx-post.*lp.*module.*approve"
    - from: "app/ui/router.py"
      to: "app/ui/router.py (_run_generation)"
      via: "lp_copy stored at generation time"
      pattern: "lp_copy"
---

<objective>
Add LP integration columns to the database, create the LP review page with per-module approve/reject cards, and wire the stage gate that locks LP review until the video pipeline is approved.

Purpose: LP-08 (per-module review) requires DB schema changes and a new review UI. The stage gate (success criterion 4) needs the `ugc_job_id` FK to check video approval status.

Output: Migration 007, updated LandingPage model, LP review page with 4 module cards, module approve endpoint, stage gate check.
</objective>

<execution_context>
@/Users/shiniguchi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shiniguchi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-lp-integration/25-RESEARCH.md

@app/models.py
@app/ui/router.py
@app/ui/templates/ugc_review.html
@app/ui/templates/partials/ugc_stage_controls.html
@app/schemas.py
@alembic/versions/006_ugcjob_schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LP integration columns to LandingPage model and create migration</name>
  <files>
    app/models.py
    alembic/versions/007_lp_integration_schema.py
    app/ui/router.py
  </files>
  <action>
1. In `app/models.py`, add these columns to the `LandingPage` class (after `deployed_url`):

```python
ugc_job_id = Column(Integer, ForeignKey("ugc_jobs.id"), nullable=True)
lp_module_approvals = Column(JSON, nullable=True)  # {"headline": "approved", "hero": "pending", ...}
lp_hero_image_path = Column(String(1000), nullable=True)  # frame from approved video
lp_hero_candidate_path = Column(String(1000), nullable=True)  # regenerated candidate
lp_review_locked = Column(Boolean, default=True)  # unlocked when UGCJob.status == "approved"
lp_copy = Column(JSON, nullable=True)  # LandingPageCopy.model_dump() stored at generation time
```

Add `Boolean` to the imports if not already there (it is — check first line).

2. Create `alembic/versions/007_lp_integration_schema.py`:
   - `revision = '007'`, `down_revision = '006'`
   - `upgrade()`: six `op.add_column('landing_pages', ...)` calls — all nullable. `lp_review_locked` gets `server_default='true'`.
   - `downgrade()`: six `op.drop_column` calls in reverse order.
   - Follow exact pattern from migration 006.

3. In `app/ui/router.py`, update `_run_generation()` to store `lp_copy` at generation time:
   - After `result = await generate_landing_page(lp_request, use_mock=use_mock)` and before saving to DB, also call `generate_copy` to get the `LandingPageCopy` object. BUT — `generate_landing_page` already calls `generate_copy` internally and doesn't return it.
   - Simpler approach: import `generate_copy` directly and call it to populate `lp_copy`. However this duplicates the LLM call.
   - Best approach: After the LP record is created, read back the copy from the generated HTML is fragile. Instead, add a `copy` field to `LandingPageResult` in `app/schemas.py` (`copy: Optional[dict] = None`) and have `generate_landing_page()` return `copy.model_dump()` in it.
   - In `app/schemas.py`: Add `copy: Optional[dict] = None` to `LandingPageResult`.
   - In `app/services/landing_page/generator.py`: After `copy = generate_copy(...)`, pass `copy=copy.model_dump()` to the `LandingPageResult` constructor.
   - In `app/ui/router.py` `_run_generation()`: Set `lp_record.lp_copy = result.copy` when creating the `LandingPage` row.

Files also modified: `app/schemas.py`, `app/services/landing_page/generator.py`
  </action>
  <verify>
    Run `cd /Users/shiniguchi/development/short-video-generator && python -c "from app.models import LandingPage; print([c.name for c in LandingPage.__table__.columns if 'lp_' in c.name or 'ugc_' in c.name])"` — should list all 6 new columns.
    Run `python -c "from app.schemas import LandingPageResult; print('copy' in LandingPageResult.model_fields)"` — should print True.
  </verify>
  <done>LandingPage model has ugc_job_id, lp_module_approvals, lp_hero_image_path, lp_hero_candidate_path, lp_review_locked, lp_copy columns. Migration 007 exists. LandingPageResult includes copy field. generate_landing_page returns copy data. _run_generation stores lp_copy in DB.</done>
</task>

<task type="auto">
  <name>Task 2: Create LP review page with module cards, approve endpoint, and stage gate</name>
  <files>
    app/ui/router.py
    app/ui/templates/lp_review.html
    app/ui/templates/partials/lp_stage_controls.html
  </files>
  <action>
1. In `app/ui/router.py`, define the LP module list (single source of truth):
```python
LP_MODULES = ["headline", "hero", "cta", "benefits"]
```

2. Add GET `/ui/lp/{run_id}/review` route:
   - Query `LandingPage` by `run_id`. 404 if not found.
   - If `lp.ugc_job_id` is set, query `UGCJob` by that ID. Set `video_approved = (ugc_job.status == "approved")`.
   - If `lp.ugc_job_id` is None, set `video_approved = True` (standalone LP, no gate).
   - Build `lp_module_content` dict from `lp.lp_copy`:
     - `"headline"` -> `lp_copy.get("headline", "") + "\n" + lp_copy.get("subheadline", "")`
     - `"hero"` -> hero image path display (use `lp.lp_hero_image_path` if set, else "No hero image")
     - `"cta"` -> `lp_copy.get("cta_text", "") + "\n" + (lp_copy.get("urgency_text", "") or "")`
     - `"benefits"` -> format benefits list as bullet text
   - Render `lp_review.html` with context: `lp`, `video_approved`, `LP_MODULES` as `modules`, `lp_module_content`.

3. Add POST `/ui/lp/{run_id}/module/{module}/approve` route:
   - Validate `module in LP_MODULES`. 400 if not.
   - Query `LandingPage` by `run_id`. 404 if not found.
   - Check `lp.lp_review_locked` — if True, return 400 "LP review is locked".
   - Update `lp.lp_module_approvals` dict: set `module` key to `"approved"`. Use `dict(lp.lp_module_approvals or {})` pattern to avoid mutating in place. Reassign to trigger SQLAlchemy dirty detection.
   - Commit.
   - Return `partials/lp_stage_controls.html` partial with context: `lp`, `modules` (LP_MODULES).

4. Create `app/ui/templates/lp_review.html` — extends `base.html`:
   - Page header: LP product idea + status badge.
   - Back link to `/ui/` (LP list).
   - Stage gate check: `{% if not video_approved %}` show locked message, `{% else %}` show module cards.
   - Module cards: iterate `modules`, for each:
     - `stage-card` div with `card-label` = module title.
     - `card-value`: if module == "hero" and `lp.lp_hero_image_path`, show `<img>` with `media_url` filter. Otherwise show text from `lp_module_content[module]`.
     - Approval status badge: check `lp.lp_module_approvals` dict for module. If `"approved"`, show green badge. If `"pending"` or missing, show approve button.
     - Approve button: `hx-post="/ui/lp/{{ lp.run_id }}/module/{{ module }}/approve"` targeting `#lp-stage-controls` with `hx-swap="outerHTML"`.
   - Include `partials/lp_stage_controls.html` at bottom.
   - Follow the exact CSS class patterns from `ugc_review.html`: `page-header`, `card-grid`, `stage-card`, `media-card`, `card-label`, `card-value`, `status-badge`, `btn btn-primary`.

5. Create `app/ui/templates/partials/lp_stage_controls.html`:
   - `<div id="lp-stage-controls">`
   - Check if all modules in `lp.lp_module_approvals` are `"approved"` (and all 4 modules present). If yes, show "LP review complete" success message.
   - Otherwise show count: "N/4 modules approved".
   - If lp has a `deployed_url`, show link to it.

6. Add a link on the UGC review page (`ugc_review.html`) for approved jobs: After the stage controls, if `job.status == "approved"`, show a "Generate LP" link/button pointing to a new route (handled in plan 25-02). For now, just add a placeholder "View LP" link that points to `/ui/lp/{run_id}/review` — but only if a linked LP exists. Since we don't have that info in the template yet, defer the link to plan 25-02.
  </action>
  <verify>
    Run `cd /Users/shiniguchi/development/short-video-generator && python -c "from app.ui.router import LP_MODULES; print(LP_MODULES)"` — should print `['headline', 'hero', 'cta', 'benefits']`.
    Verify template exists: `ls app/ui/templates/lp_review.html app/ui/templates/partials/lp_stage_controls.html`.
    Run `python -c "from app.ui.router import router; routes = [r.path for r in router.routes]; print('/lp/{run_id}/review' in routes, '/lp/{run_id}/module/{module}/approve' in routes)"` — both True.
  </verify>
  <done>LP review page renders 4 module cards from lp_copy data. Module approve endpoint updates lp_module_approvals JSON. Stage gate blocks review when video not approved. LP stage controls partial shows approval progress.</done>
</task>

</tasks>

<verification>
1. `python -c "from app.models import LandingPage; print('lp_copy' in [c.name for c in LandingPage.__table__.columns])"` returns True
2. `python -c "from app.ui.router import LP_MODULES; print(len(LP_MODULES))"` returns 4
3. Template files exist at expected paths
4. Routes registered correctly in router
</verification>

<success_criteria>
- LandingPage model has all 6 new columns
- Migration 007 exists and follows project pattern
- LP review page shows 4 module cards with content from lp_copy
- Module approve endpoint updates lp_module_approvals per-module
- Stage gate locks LP review when linked UGCJob status != "approved"
- LP copy is persisted at generation time via updated pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/25-lp-integration/25-01-SUMMARY.md`
</output>
