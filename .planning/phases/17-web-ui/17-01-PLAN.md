---
phase: 17-web-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models.py
  - app/schemas.py
  - alembic/versions/005_landing_pages_schema.py
  - app/ui/__init__.py
  - app/ui/router.py
  - app/ui/static/ui.css
  - app/ui/static/ui.js
  - app/ui/templates/base.html
  - app/main.py
  - requirements.txt
autonomous: true

must_haves:
  truths:
    - "LandingPage model exists in database with run_id, product_idea, status, html_path columns"
    - "Alembic migration 005 creates landing_pages table"
    - "GET /ui/ returns an HTML page (200 status)"
    - "Static files served at /ui/static/ui.css and /ui/static/ui.js"
    - "python-multipart is listed in requirements.txt"
  artifacts:
    - path: "app/models.py"
      provides: "LandingPage SQLAlchemy model"
      contains: "class LandingPage"
    - path: "alembic/versions/005_landing_pages_schema.py"
      provides: "Migration for landing_pages table"
      contains: "landing_pages"
    - path: "app/ui/router.py"
      provides: "Web UI router with Jinja2Templates"
      contains: "router = APIRouter"
    - path: "app/ui/templates/base.html"
      provides: "Base HTML layout for all UI pages"
      contains: "<html"
    - path: "app/main.py"
      provides: "UI router and static mounts"
      contains: "ui-static"
  key_links:
    - from: "app/main.py"
      to: "app/ui/router.py"
      via: "include_router"
      pattern: "app\\.include_router.*ui"
    - from: "app/main.py"
      to: "app/ui/static/"
      via: "StaticFiles mount"
      pattern: "StaticFiles.*ui/static"
---

<objective>
Add LandingPage DB model, migration, and web UI scaffold (router + templates + static files).

Purpose: Foundation for all Phase 17 features — the model tracks LP status for listing/deployment, and the UI scaffold provides the router + template system for all pages.
Output: LandingPage model + migration 005, app/ui/ package with router, base template, CSS/JS, mounted in main.py.
</objective>

<execution_context>
@/Users/shiniguchi/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shiniguchi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-web-ui/17-RESEARCH.md
@app/models.py
@app/main.py
@app/schemas.py
@requirements.txt
@alembic/versions/004_waitlist_schema.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LandingPage model, migration, and python-multipart</name>
  <files>app/models.py, alembic/versions/005_landing_pages_schema.py, requirements.txt</files>
  <action>
1. Add `LandingPage` model to `app/models.py` after `WaitlistEntry`:
   - id: Integer primary key
   - run_id: String(50), nullable=False, unique=True
   - product_idea: String(500), nullable=False
   - target_audience: String(500), nullable=True
   - html_path: String(1000), nullable=True
   - status: String(50), default="generated" (values: generated, deployed, archived)
   - color_scheme_source: String(50), nullable=True
   - sections: JSON, nullable=True
   - created_at: DateTime(timezone=True), server_default=func.now()
   - deployed_at: DateTime(timezone=True), nullable=True
   - deployed_url: String(1000), nullable=True

2. Create `alembic/versions/005_landing_pages_schema.py` following the same pattern as `004_waitlist_schema.py`:
   - revision id: generate a random 12-char hex string
   - down_revision: read the revision id from 004_waitlist_schema.py
   - upgrade(): create "landing_pages" table with all columns above, plus UniqueConstraint on run_id
   - downgrade(): drop "landing_pages" table

3. Add `python-multipart` to `requirements.txt` (required for FastAPI Form() parsing). Add it after `email-validator` line.

4. Run `pip install python-multipart` in the venv.
  </action>
  <verify>
- `python -c "from app.models import LandingPage; print(LandingPage.__tablename__)"` prints "landing_pages"
- `python -c "import multipart; print('ok')"` prints "ok"
- `grep python-multipart requirements.txt` matches
- `python -c "from alembic.versions import *"` does not error (migration file is valid Python)
  </verify>
  <done>LandingPage model in models.py, migration 005 creates landing_pages table, python-multipart installed and in requirements.txt</done>
</task>

<task type="auto">
  <name>Task 2: Create web UI scaffold with router, templates, static files, and main.py mounts</name>
  <files>app/ui/__init__.py, app/ui/router.py, app/ui/static/ui.css, app/ui/static/ui.js, app/ui/templates/base.html, app/main.py</files>
  <action>
1. Create `app/ui/__init__.py` — empty init for package.

2. Create `app/ui/router.py`:
   - Import APIRouter, Request, Jinja2Templates from fastapi
   - `router = APIRouter(prefix="/ui", tags=["web-ui"])`
   - `templates = Jinja2Templates(directory=str(Path(__file__).parent / "templates"))`
   - Add placeholder `GET /ui/` route that returns `index.html` template with empty context:
     ```python
     @router.get("/", response_class=HTMLResponse)
     async def dashboard(request: Request):
         return templates.TemplateResponse(request=request, name="index.html", context={"lps": []})
     ```
   - This placeholder will be expanded in Plan 02 and 03.

3. Create `app/ui/templates/base.html` — minimal HTML5 base layout:
   - DOCTYPE, html, head with viewport meta, title "ViralForge", link to /ui/static/ui.css
   - Body with simple nav bar (h1 "ViralForge" linking to /ui/, nav links: "Dashboard" → /ui/, "Generate" → /ui/generate)
   - `{% block content %}{% endblock %}` for page content
   - Script tag for /ui/static/ui.js at end of body

4. Create `app/ui/templates/index.html` — extends base.html:
   - Block content: heading "Landing Pages", placeholder message "No LPs generated yet. Generate your first LP."
   - This is expanded in Plan 03 with LP list table.

5. Create `app/ui/static/ui.css` — minimal clean styles:
   - CSS reset (box-sizing, margin/padding 0), system font stack
   - Nav bar: dark background (#1a1a2e), white text, flex layout
   - Main content: max-width 960px, centered, padding
   - Basic button styles, form input styles (will be used in Plan 02)
   - Keep it under 80 lines — functional, not fancy

6. Create `app/ui/static/ui.js` — minimal JS placeholder:
   - Comment: "Web UI client-side logic — expanded in Plan 02 (SSE) and Plan 03 (deploy)"
   - Empty for now (Plan 02 adds SSE client code)

7. Update `app/main.py`:
   - Add imports: `from fastapi.staticfiles import StaticFiles` and `from app.ui import router as ui_router`
   - Add `from pathlib import Path` if not present
   - BEFORE the existing `app.include_router(routes.router)` line, add:
     ```python
     # Web UI — static files + HTML router
     app.mount("/ui/static", StaticFiles(directory=str(Path(__file__).parent / "ui" / "static")), name="ui-static")
     app.include_router(ui_router.router)
     ```
   - Also mount output directory for LP preview (used in Plan 03):
     ```python
     # Serve generated LP files for preview
     _output_dir = Path(__file__).parent.parent / "output"
     _output_dir.mkdir(exist_ok=True)
     app.mount("/output", StaticFiles(directory=str(_output_dir)), name="lp-output")
     ```
   - Keep existing `app.include_router(routes.router)` AFTER ui mounts (order matters for route priority).

IMPORTANT: Use `str(Path(__file__).parent / ...)` pattern for all directory paths — don't hardcode relative strings. This ensures paths resolve correctly regardless of working directory.
  </action>
  <verify>
- Start server: `cd /Users/shiniguchi/development/short-video-generator && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000` (should start without errors)
- `curl -s http://localhost:8000/ui/ | grep "ViralForge"` returns HTML with ViralForge title
- `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/ui/static/ui.css` returns 200
- `curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/ui/static/ui.js` returns 200
- `curl -s http://localhost:8000/health` still returns JSON (existing API not broken)
  </verify>
  <done>Web UI accessible at /ui/ with base template, nav bar, empty dashboard page. Static CSS/JS served. Existing API routes still work.</done>
</task>

</tasks>

<verification>
1. LandingPage model importable: `from app.models import LandingPage`
2. Migration file exists and has correct down_revision chain
3. Server starts without import errors
4. GET /ui/ returns HTML with nav and content
5. Static assets served at /ui/static/*
6. GET /health still works (no route conflicts)
7. python-multipart installed and in requirements.txt
</verification>

<success_criteria>
- LandingPage model with all columns exists in models.py
- Migration 005 creates landing_pages table
- python-multipart in requirements.txt
- /ui/ serves an HTML page with navigation
- /ui/static/ui.css and /ui/static/ui.js return 200
- Existing /health endpoint still works
</success_criteria>

<output>
After completion, create `.planning/phases/17-web-ui/17-01-SUMMARY.md`
</output>
